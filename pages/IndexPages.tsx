import Head from 'next/head';

export default function IndexPages() {
    return (
        <Head>
            <title>Media Social Dashboard</title>
            <meta name="Viewport" content="width=device-width, initial-scale=1.0"/>
        </Head>
    )
}